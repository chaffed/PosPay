.claude/
.venv/
__pycache__/
*.pyc
*.db
.env
ml_artifacts/
.pytest_cache/
*.egg-info/
data/
.pospay-run/
# scripts/generate_keys.py's default output dir for real signing keys — never commit
# these. dev_keys/ (the checked-in, deliberately public test key pair) is NOT ignored.
/keys/
